# Create an OAuth client at https://osu.ppy.sh/home/account/edit (the "OAuth" section).
# The callback URL can stay empty: we use the client_credentials grant.
# Copy this file to .env and fill in your values; .env is ignored by git.
OSU_CLIENT_ID=
OSU_CLIENT_SECRET=
